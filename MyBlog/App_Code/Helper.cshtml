@helper Tags(System.Web.Mvc.HtmlHelper htmlHelper, ICollection<MyBlog.Core.Entities.Tag> tags)
{
foreach (var tag in tags)
{
        <div class="tags-div">
            @MyBlog.ActionLinkExtensions.TagLink(htmlHelper, tag)
        </div>
}
}